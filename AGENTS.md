# Repository Instructions

## 기본 원칙

- 이 레포는 백엔드 포트폴리오용 프로젝트이므로 기능 구현만 하지 말고 문제 정의, 설계 의도, 검증 방법, 측정 근거를 함께 남긴다.
- 이슈 기반으로 작업한다. 작업을 시작하기 전에 관련 GitHub Issue를 확인하고, 구현 범위가 이슈의 완료 기준과 맞는지 확인한다.
- 관련 없는 리팩토링, 포맷팅, 문서 수정은 피한다. 필요한 변경만 작게 나누어 진행한다.
- 기존 사용자 변경사항을 되돌리지 않는다.

## 브랜치와 커밋

- 작업 브랜치는 이슈 단위로 만든다.
- 브랜치 이름은 가능하면 `feature/#{issue-number}/{short-name}`, `fix/{issue-number}/{short-name}`, `docs/{issue-number}/{short-name}` 형식을 사용한다.
- 커밋은 사용자가 명시적으로 요청했을 때만 작성한다.
- 커밋 메시지는 한글로 작성하고 prefix를 붙인다.

예시:

```text
feat: 코스 알림받기 API 추가
fix: 중복 알림 생성 방지
docs: 알림 서비스 설계 문서 추가
test: 알림 멱등성 테스트 추가
```

## PR 작성 기준

PR에는 다음 내용을 남긴다.

- 어떤 문제를 해결하려는지
- 왜 이 방식으로 설계했는지
- 구현한 주요 변경사항
- 테스트 결과
- 성능 또는 운영 관점에서 확인한 측정 근거
- 남은 리스크 또는 후속 작업

성능 수치는 추정하지 말고 실제 측정한 값만 적는다.

## 검증 기준

- 기능 테스트, 동시성 테스트, 멱등성 테스트를 우선한다.
- 성능 개선 또는 비동기 처리 변경은 가능하면 p95 latency, 처리 지연, 실패 수, DB connection active/pending 같은 지표로 검증한다.
- 테스트를 실행하지 못했다면 PR에 이유를 명확히 남긴다.

## 금지 사항

- 근거 없는 성능 개선 수치를 적지 않는다.
- 문서와 코드 상태가 불일치한 채로 두지 않는다.
- `Co-authored-by`, `Codex`, `OpenAI`, `ChatGPT` 등 assistant/vendor attribution을 커밋, PR, 문서, 코드에 추가하지 않는다.
